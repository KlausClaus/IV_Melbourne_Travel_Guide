* GEOM90007 IV Porject Melbourne Travel Guide
** 1.Overview
By allowing users to select from three different datasets (Landmarks, Toilets, and Top 50 Restaurants), it provides immediate, context-sensitive information, addressing the common challenges tourists face in terms of finding attractions, amenities, and dining.

** 2.Environment set up
In R language, you can use the install.packages() function to install the required packages. Here are the instructions on how to install the libraries you mentioned:
```bash
install.packages("shiny")
install.packages("leaflet")
install.packages("readr")
install.packages("readxl")
install.packages("bslib")
install.packages("httr")
install.packages("jsonlite")
install.packages("ggplot2")
install.packages("plotly")
install.packages("shinyjs")
install.packages("ggiraph")
```
## 3. How to run the code
You can open the touristApp.R in the file through the RStudio, and simply click "Run App" at the top-right corner of the code window
Hint:
You need to keep all the required files in the same folder, which we have already uploaded.
 